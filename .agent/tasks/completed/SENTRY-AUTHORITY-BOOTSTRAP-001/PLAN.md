# Bootstrap Plan

1. Reconcile SENTRY Notion, the canonical Authority package, GitHub, and the live checkout.
2. Install the canonical root router and reusable `.agents/` workflow/reference files.
3. Initialize `.agent/` from actual SENTRY scope and repository reality.
4. Preserve the task packet as completed provenance after validation.
5. Validate structure, references, project-specific content, scope, diff, commit, push, and Notion update.
