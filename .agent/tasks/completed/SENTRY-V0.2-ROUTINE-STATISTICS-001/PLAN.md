# Plan

1. Inspect accepted V0.1/resident-runtime baseline, history, timezone, storage,
   and systemd conventions.
2. Add the four fixed routine types, schema-v5 snapshots, CLI, API endpoint,
   configuration, and independent systemd refresh timer.
3. Add deterministic fixtures for maturity, dates, circular time, robust
   statistics, exclusions, DST, idempotence, restore, and privacy.
4. Refresh the real production database without seeded history; verify sparse
   evidence remains honestly `insufficient`.
5. Verify resident health, timer execution, integrity, regression, and records;
   commit and push the qualified foundation.
