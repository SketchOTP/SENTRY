# Plan

1. Verify accepted baseline and current host/service state.
2. Add proactive watch mode, user-systemd units, production-config installer, and focused tests.
3. Install and enable the units for the authenticated `sketch` user session.
4. Run a bounded supervised live window and exercise isolated component restarts.
5. Run end-to-end regression, update Authority/Notion, commit/push, and close the packet.

## Stop conditions

- Atlas mount or canonical checkout becomes unstable.
- A service restart creates false physical events or duplicate sessions/actions.
- The host requires unapproved privileged changes.
- A failure requires detector, tracker, identity, storage topology, or voice redesign.
