# Handoff

Release handoff is pending final exact-code regression and CI. The candidate
preserves the restricted resident profile, persistent Codex thread, dual-Vosk
streaming command evidence, one sample-indexed PCM timeline, Whisper final STT,
trusted current-turn execution broker, exceptional deferred-action dialogue,
private append-only audit, and British Kokoro voice.

Live trusted-action proof passed: the operator said to move `move-test.txt` to
Downloads; Vosk/Whisper/Codex resolved the exact request, the host executed once
without a redundant confirmation, source and destination hashes matched, and
the audit recorded `direct_current_turn`. The operator explicitly waived
artificial deferred approval/cancellation/revision live drills after that pass;
their state machine, timing, natural-language interpretation, replay, and
fail-closed behavior remain automated-test requirements rather than claimed
live observations.
