# Handoff

Disposition: **CONTINUE — push-first mandatory-alert latency slice complete;
full SENTRY V0.4 Goal remains in progress**.

The resident voice process now blocks on ANIMA's authenticated request-ready
wait rather than polling every 15 seconds. Empty waits keep the orb idle and do
not start Codex. Eligible work is claimed by priority; health, sparse context and
the frozen request-specific tool catalogue are preloaded before durable
provider-start. A canonical owner-mandated factual alert begins through warm
Kokoro immediately after provider-start while low-effort contextual reasoning
runs in parallel. Successful autonomous speech opens the normal follow-up
listening window.

PC and RPi playback owners now return exact `tts_start_at` timestamps. The
private timing-only trial ledger records the three-second mandatory-audible and
twelve-second contextual objectives without announcement text, model output or
household payload. The live voice and projection services are deployed. A
shared resident Codex app-server remains blocked because the installed runtime
cannot safely rotate per-event MCP bindings/catalogues or guarantee stale child
teardown; retaining the per-event process boundary preserves fencing and
no-replay. Deterministic target/regression evidence is complete; one fresh
physical event timing sample on this exact source remains pending.
